# Netlify redirects file

# Proxy all requests to the PSU server through our function
/*    /.netlify/functions/proxy/:splat    200

# Keep the main page accessible
/    /index.html    200

# Handle POST requests specifically
/*    /.netlify/functions/proxy/:splat    200! 